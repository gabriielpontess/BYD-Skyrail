import { documentViewerService } from './documents/viewer-service.js';
import { syncAll } from './sync.js';

const $=(selector,root=document)=>root.querySelector(selector);
const $$=(selector,root=document)=>[...root.querySelectorAll(selector)];

const modalState=new WeakMap();
const FOCUSABLE='button:not([disabled]),a[href],input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex="-1"])';
const CLOSE_SELECTOR='[data-close],[data-ux-close],[data-import-close],[data-packager-close],[data-pdf-close]';
const NON_MODAL_ASYNC_ACTIONS='[data-home-action="sync"],[data-sync],[data-refresh]';
const CRITICAL_PAGE_FORMS=':is(#profile-form,#password-form)[data-operation-running="1"]';
let routeRepairScheduled=false,avatarGeneration=0,avatarToastTimer=null;

const originalViewerOpen=documentViewerService.open.bind(documentViewerService);
let viewerOpening=false;
documentViewerService.open=async function(id){const active=$('.local-pdf-backdrop');if(viewerOpening||active){active?.querySelector('[data-pdf-stage]')?.focus?.({preventScroll:true});return false}viewerOpening=true;try{return await originalViewerOpen(id)}finally{viewerOpening=false}};
function cachedMember(){try{return JSON.parse(localStorage.getItem('byd-skyrail-member-cache')||'null')}catch{return null}}
function currentRole(){return String(cachedMember()?.role||'').toUpperCase()}
function routeName(){return(location.hash.replace(/^#\/?/,'').split('?')[0]||'home')}
function routeAllowed(role,route){if(['home','documents','profile'].includes(route))return true;if(route==='audit')return role==='ADMIN';if(route==='controller-updates')return role==='CONTROLLER';return false}
function repairStaleRouteSurface(){const page=$('#page');if(!page||routeRepairScheduled)return;const route=routeName();const staleAudit=route!=='audit'&&Boolean(page.querySelector('.admin-tabs,#admin-content'));const staleController=route!=='controller-updates'&&Boolean(page.querySelector('.controller-update-card'));const staleDocuments=route!=='documents'&&Boolean(page.querySelector('.local-document-search-panel,[data-local-layout]'));if(!staleAudit&&!staleController&&!staleDocuments)return;routeRepairScheduled=true;queueMicrotask(()=>{routeRepairScheduled=false;window.dispatchEvent(new HashChangeEvent('hashchange'))})}
function sanitizeRestrictedUi(){const member=cachedMember();if(!member||!$('#page'))return;const role=currentRole(),route=routeName();$$('[data-document-packager]').forEach(button=>{if(!(role==='ADMIN'&&route==='audit'))button.remove()});$$('[data-local-import]').forEach(button=>{const allowed=(role==='ADMIN'&&route==='audit')||(role==='CONTROLLER'&&route==='controller-updates');if(!allowed)button.remove()});if(!routeAllowed(role,route)&&location.hash!=='#/home'){location.hash='#/home';return}repairStaleRouteSurface()}
function isVisible(element){if(!element?.isConnected||element.classList.contains('hidden'))return false;const style=getComputedStyle(element);return style.display!=='none'&&style.visibility!=='hidden'}
function modalBusy(backdrop){return backdrop?.dataset.operationRunning==='1'||Boolean(backdrop?.querySelector('.is-loading,[aria-busy="true"]'))}
function visibleModals(){return $$('.modal-backdrop').filter(isVisible)}
function accessibleDialog(backdrop){const dialog=$('[role="dialog"],.modal',backdrop);if(!dialog)return null;if(!dialog.hasAttribute('role'))dialog.setAttribute('role','dialog');dialog.setAttribute('aria-modal','true');if(!dialog.hasAttribute('aria-label')&&!dialog.hasAttribute('aria-labelledby')){const heading=$('.modal-head-copy strong,h1,h2,h3',dialog)?.textContent?.trim();dialog.setAttribute('aria-label',heading||'Janela do BYD Skyrail')}return dialog}
function modalRouteAllowed(backdrop){const route=routeName(),role=currentRole();const adminForm=backdrop.querySelector('#document-admin-form,#user-admin-form');const heading=$('.modal-head-copy strong',backdrop)?.textContent?.trim()||'';const adminHistory=/^Histórico\s*·/i.test(heading);if(adminForm||adminHistory)return role==='ADMIN'&&route==='audit';return true}
function focusables(backdrop){return $$(FOCUSABLE,backdrop).filter(element=>isVisible(element)&&!element.closest('[inert]'))}
function wrapAsyncForms(backdrop){$$('form',backdrop).forEach(form=>{if(form.dataset.systemicAsyncGuard==='1'||typeof form.onsubmit!=='function')return;form.dataset.systemicAsyncGuard='1';const original=form.onsubmit;form.onsubmit=async function(event){if(backdrop.dataset.operationRunning==='1'){event?.preventDefault?.();event?.stopImmediatePropagation?.();return}backdrop.dataset.operationRunning='1';try{return await original.call(this,event)}finally{backdrop.dataset.operationRunning='0'}}})}
function wrapPageForms(root=document){$$('form',root).forEach(form=>{if(form.closest('.modal-backdrop')||form.dataset.systemicPageAsyncGuard==='1'||typeof form.onsubmit!=='function')return;form.dataset.systemicPageAsyncGuard='1';const original=form.onsubmit;form.onsubmit=async function(event){if(form.dataset.operationRunning==='1'){event?.preventDefault?.();event?.stopImmediatePropagation?.();return false}form.dataset.operationRunning='1';form.setAttribute('aria-busy','true');try{return await original.call(this,event)}finally{form.dataset.operationRunning='0';form.setAttribute('aria-busy','false')}}})}
function avatarToast(message,error=false){const toast=$('#toast');if(!toast)return;clearTimeout(avatarToastTimer);toast.textContent=message;toast.classList.toggle('error',error);toast.classList.add('show');avatarToastTimer=setTimeout(()=>toast.classList.remove('show'),3600)}
async function runOfflineLocalSync(){try{const result=await syncAll();avatarToast(`Catálogo local verificado: ${result.total.toLocaleString('pt-BR')} documento(s).`);window.dispatchEvent(new HashChangeEvent('hashchange'));return result}catch(error){avatarToast(error?.message||'Não foi possível verificar o catálogo local.',true);return false}}
function wrapPageAsyncActions(root=document){$$(NON_MODAL_ASYNC_ACTIONS,root).forEach(button=>{if(button.dataset.systemicAsyncClickGuard==='1'||typeof button.onclick!=='function')return;button.dataset.systemicAsyncClickGuard='1';const original=button.onclick;button.onclick=async function(event){if(button.dataset.operationRunning==='1'){event?.preventDefault?.();event?.stopImmediatePropagation?.();return false}button.dataset.operationRunning='1';button.setAttribute('aria-busy','true');button.disabled=true;try{const isLocalSync=button.matches('[data-sync],[data-home-action="sync"]');if(isLocalSync&&!navigator.onLine)return await runOfflineLocalSync();return await original.call(this,event)}finally{if(button.isConnected){button.dataset.operationRunning='0';button.setAttribute('aria-busy','false');button.disabled=false}}}})}
function criticalOperationRunning(){if($(CRITICAL_PAGE_FORMS))return true;if($('[data-refresh][data-operation-running="1"]'))return true;return visibleModals().some(modal=>modalBusy(modal))}
function guardLogout(event){const logout=event.target.closest?.('[data-logout]');if(!logout||!criticalOperationRunning())return;event.preventDefault();event.stopImmediatePropagation();avatarToast('Aguarde a operação em andamento terminar antes de sair.',true)}
function resizeAvatarSafe(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onerror=reject;reader.onload=()=>{const image=new Image();image.onerror=reject;image.onload=()=>{try{const size=Math.min(image.width,image.height);if(!size)throw new Error('Imagem vazia.');const canvas=document.createElement('canvas');canvas.width=240;canvas.height=240;const context=canvas.getContext('2d');if(!context)throw new Error('Canvas indisponível.');context.drawImage(image,(image.width-size)/2,(image.height-size)/2,size,size,0,0,240,240);resolve(canvas.toDataURL('image/jpeg',.82))}catch(error){reject(error)}};image.src=reader.result};reader.readAsDataURL(file)})}
function guardAvatarInput(root=document){const inputs=[];if(root instanceof Element&&root.matches('#avatar-input'))inputs.push(root);inputs.push(...$$('#avatar-input',root));inputs.forEach(input=>{if(input.dataset.systemicAvatarGuard==='1')return;input.dataset.systemicAvatarGuard='1';input.onchange=async event=>{const file=event.currentTarget.files?.[0];if(!file)return;if(!['image/jpeg','image/png'].includes(file.type)||file.size>5*1024*1024){if(routeName()==='profile')avatarToast('Use uma imagem JPG ou PNG de até 5MB.',true);return}const memberAtStart=cachedMember(),userId=memberAtStart?.user_id;if(!userId)return;const generation=++avatarGeneration;input.dataset.operationRunning='1';input.setAttribute('aria-busy','true');try{const dataUrl=await resizeAvatarSafe(file);if(generation!==avatarGeneration)return;localStorage.setItem(`byd-skyrail:avatar:${userId}`,dataUrl);const stillCurrent=cachedMember()?.user_id===userId&&routeName()==='profile';if(!stillCurrent)return;const profilePhoto=$('#profile-photo'),headerAvatar=$('.user-chip .avatar');if(!profilePhoto||!headerAvatar)return;profilePhoto.innerHTML=`<img src="${dataUrl}" alt="Foto do perfil">`;headerAvatar.innerHTML=`<img src="${dataUrl}" alt="Foto do perfil">`;avatarToast('Foto atualizada neste dispositivo.')}catch{if(generation===avatarGeneration&&cachedMember()?.user_id===userId&&routeName()==='profile')avatarToast('Não foi possível processar a foto.',true)}finally{if(input.isConnected){input.dataset.operationRunning='0';input.setAttribute('aria-busy','false')}}}})}
function scanPageAsync(root=document){wrapPageForms(root);wrapPageAsyncActions(root);guardAvatarInput(root)}
function syncModalStack(){const open=visibleModals(),top=open.at(-1)||null,app=$('#app');if(app)app.inert=Boolean(top);open.forEach(backdrop=>{const isTop=backdrop===top;backdrop.inert=!isTop;backdrop.setAttribute('aria-hidden',String(!isTop))});$$('.modal-backdrop').filter(backdrop=>!open.includes(backdrop)).forEach(backdrop=>{backdrop.inert=false;backdrop.setAttribute('aria-hidden','true')});document.body.classList.toggle('has-modal-open',Boolean(top))}
function onVisibilityChange(backdrop){const state=modalState.get(backdrop);if(!state)return;const visible=isVisible(backdrop);if(visible===state.visible){syncModalStack();return}state.visible=visible;if(visible){const active=document.activeElement;state.opener=active instanceof HTMLElement&&!backdrop.contains(active)?active:null;accessibleDialog(backdrop);wrapAsyncForms(backdrop);syncModalStack();requestAnimationFrame(()=>{if(!isVisible(backdrop)||visibleModals().at(-1)!==backdrop)return;const target=focusables(backdrop)[0]||accessibleDialog(backdrop);if(target instanceof HTMLElement){if(!target.matches(FOCUSABLE)&&!target.hasAttribute('tabindex'))target.tabIndex=-1;target.focus({preventScroll:true})}})}else{syncModalStack();const opener=state.opener;state.opener=null;if(opener?.isConnected)requestAnimationFrame(()=>opener.focus({preventScroll:true}))}}
function registerModal(backdrop){if(!(backdrop instanceof HTMLElement)||modalState.has(backdrop))return;if(!modalRouteAllowed(backdrop)){backdrop.remove();syncModalStack();return}const startsVisible=isVisible(backdrop),state={visible:false,opener:null,observer:null};modalState.set(backdrop,state);accessibleDialog(backdrop);wrapAsyncForms(backdrop);backdrop.setAttribute('aria-hidden',String(!startsVisible));state.observer=new MutationObserver(()=>onVisibilityChange(backdrop));state.observer.observe(backdrop,{attributes:true,attributeFilter:['class','hidden']});if(startsVisible)onVisibilityChange(backdrop);else syncModalStack()}
function scanModals(root=document){if(root instanceof Element&&root.matches('.modal-backdrop'))registerModal(root);root.querySelectorAll?.('.modal-backdrop').forEach(registerModal);syncModalStack()}
document.addEventListener('click',guardLogout,true);document.addEventListener('click',event=>{const close=event.target.closest?.(CLOSE_SELECTOR);if(!close)return;const backdrop=close.closest('.modal-backdrop');if(!backdrop||!modalBusy(backdrop))return;event.preventDefault();event.stopImmediatePropagation()},true);document.addEventListener('keydown',event=>{if(event.key!=='Tab')return;const top=visibleModals().at(-1);if(!top)return;const items=focusables(top);if(!items.length){event.preventDefault();const dialog=accessibleDialog(top);if(dialog instanceof HTMLElement){if(!dialog.hasAttribute('tabindex'))dialog.tabIndex=-1;dialog.focus({preventScroll:true})}return}const first=items[0],last=items.at(-1),active=document.activeElement;if(event.shiftKey&&(active===first||!top.contains(active))){event.preventDefault();last.focus({preventScroll:true});return}if(!event.shiftKey&&(active===last||!top.contains(active))){event.preventDefault();first.focus({preventScroll:true})}},true);
if(document.body)new MutationObserver(records=>{for(const record of records){for(const node of record.addedNodes)if(node instanceof Element){scanModals(node);scanPageAsync(node)}for(const node of record.removedNodes){if(!(node instanceof Element))continue;const nested=node.querySelectorAll?.('.modal-backdrop')||[];const removed=[node,...nested].filter(item=>item.matches?.('.modal-backdrop'));removed.forEach(backdrop=>{const state=modalState.get(backdrop);if(!state)return;state.observer?.disconnect();if(state.visible){const opener=state.opener;state.visible=false;state.opener=null;if(opener?.isConnected)requestAnimationFrame(()=>opener.focus({preventScroll:true}))}modalState.delete(backdrop)})}}syncModalStack()}).observe(document.body,{childList:true,subtree:false});
const appRoot=$('#app');if(appRoot)new MutationObserver(()=>queueMicrotask(()=>{sanitizeRestrictedUi();scanPageAsync(appRoot)})).observe(appRoot,{childList:true,subtree:true});addEventListener('hashchange',()=>{avatarGeneration++;queueMicrotask(()=>scanModals());queueMicrotask(()=>{sanitizeRestrictedUi();scanPageAsync(appRoot||document)})});addEventListener('load',()=>{scanModals();sanitizeRestrictedUi();scanPageAsync()});scanModals();sanitizeRestrictedUi();scanPageAsync();